'use strict';

var _expect = require('expect');

var _expect2 = _interopRequireDefault(_expect);

var _index = require('../index');

var _index2 = _interopRequireDefault(_index);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('toPass', function () {
  before(function () {
    _expect2.default.extend(_index2.default);
  });

  it('throws when the actual value does not pass the predicate', function () {
    (0, _expect2.default)(function () {
      (0, _expect2.default)(42).toPass(function (n) {
        return n < 40;
      });
    }).toThrow(/Expected 42 to pass/);
  });

  it('throws with custom message when the actual value does not pass the predicate', function () {
    (0, _expect2.default)(function () {
      (0, _expect2.default)(42).toPass(function (n) {
        return n < 40;
      }, 'Should be under forty');
    }).toThrow('Should be under forty');
  });

  it('does not throws when the actual value passes the predicate', function () {
    (0, _expect2.default)(function () {
      (0, _expect2.default)(42).toPass(function (n) {
        return n > 40;
      });
    }).toNotThrow();
  });
});